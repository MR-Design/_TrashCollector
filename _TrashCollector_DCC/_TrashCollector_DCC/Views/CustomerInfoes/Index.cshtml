@model IEnumerable<_TrashCollector_DCC.Models.CustomerInfo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WeeklyPickup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuspendPickupsStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuspendPickupsEnd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExtraPickup)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WeeklyPickup)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuspendPickupsStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuspendPickupsEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtraPickup)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Money }) |
                @Html.ActionLink("Details", "Details", new { id=item.Money }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Money })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
